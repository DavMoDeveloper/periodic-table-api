NODE_ENV=development

# Puerto del servidor
PORT=3000

# Origins permitidos por CORS (separa por comas)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate limit (por IP)
RL_WINDOW_MS=900000   # 15 minutos
RL_MAX_REQ=100        # 100 req por ventana

# Si usas proxies/reverse proxies, puedes ajustar otras opciones según tu despliegue
